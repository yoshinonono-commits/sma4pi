PySide6>=6.5
matplotlib>=3.7
numpy>=1.24
scipy>=1.10

# --- 開発用 -----------------------------------------------------------------
# アプリを動かすだけなら不要。exe / .app のビルド (build.bat / build.sh) にだけ使う。
pyinstaller>=6.0
